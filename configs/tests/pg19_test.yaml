data:
  dataset_name: pg19
  file_path: data/dummy/pg19.csv
  tokenizer_name: gpt2
  max_length: 512
  text_col: text

model:
  vocab_size: 50257
  embed_dim: 256
  num_layers: 4
  dropout: 0.2

training:
  batch_size: 8
  lr: 1e-4
  epochs: 2
  grad_clip: 1.0
  log_interval: 10 